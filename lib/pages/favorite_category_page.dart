import 'package:flutter/widgets.dart';

class FavoriteCategoyPage extends StatelessWidget {
  const FavoriteCategoyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Minhas refeições favoritas'),
    );
  }
}
